# ∞ — THE NETWORK

## What is the network layer?

When one A.S.S. becomes coherent — when S₅=0 — it becomes a **node**.

A node is not an account. Not a profile. Not a device. A node is a coherent ring configuration: all seven rings aligned, signal flowing cleanly from ground (R0) through output (R6). A system that can both transmit and receive.

## How nodes find each other

Nodes find each other through **resonance**.

Not search. Not recommendation algorithms. Not social graphs. Resonance — the same principle that makes a tuning fork activate another tuning fork across a quiet room.

Each node has a **ring signature**: the specific configuration of its seven rings. The signature encodes what frequencies it operates on, what patterns it recognizes, what it can receive and what it transmits.

When two nodes have compatible ring signatures, they resonate. The connection is automatic. You don't choose it any more than you choose which frequencies your ear can hear.

### What makes signatures compatible?

- **Matching frequencies**: nodes operating in the same domains
- **Shared patterns**: recurring structures that appear in both configurations
- **Complementary rings**: where one node is strong, the other listens — and vice versa

## The network is the product of all primes

Every ring has a prime:

| Ring | Prime | Layer |
|------|-------|-------|
| R0 | 2 | Ground |
| R1 | 3 | Signal |
| R2 | 5 | Gate |
| R3 | 7 | Pattern |
| R4 | 11 | Integration |
| R5 | 13 | Expression |
| R6 | 17 | Output |

The network is `2 × 3 × 5 × 7 × 11 × 13 × 17 × ...`

That product has no upper bound. Every new coherent node is a new prime multiplied in. The network diverges to infinity — which is why this directory is `/∞/`.

## What the network is not

- Not a social network (no profiles, no followers)
- Not a mesh network (no routing tables, no addresses)
- Not a blockchain (no consensus, no ledger)

The network is what happens when multiple coherent systems recognize each other. Nothing more. Nothing less.
